- form_for([:admin, @animal], :html => { :multipart => true }) do |f|
  = f.error_messages

  .row.clear
    = f.label :name
    = f.text_field :name
    
  .row.clear
    = f.file_field :avatar
    
    

  -# t.string   "name"
  -# t.string   "identifier"
  -# t.string   "kind"
  -# t.string   "image_url"
  -# t.string   "description"

  .row.clear 
    = f.submit 'Update'