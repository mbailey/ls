%h1= "Listing volunteers (#{@volunteer_count} in total)"

  = link_to 'download csv file', '/volunteers.csv'
  = link_to 'view all on map', '/volunteers/map'
  = will_paginate @volunteers, :class => 'apple_pagination'
  = render :partial => 'volunteers'
  = will_paginate @volunteers, :class => 'apple_pagination'
  = link_to 'New volunteer', new_volunteer_path
